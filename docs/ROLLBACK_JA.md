戻し方 / トラブル時

SteamVR自動開始を無効にする:
1. 00_START_VRStickScope.exe を起動します。
2. Runtime を開きます。
3. 「OSC補正の自動開始を無効化」を押します。
4. SteamVRを再起動します。

入力がおかしいと感じる場合:
1. VRChatとSteamVRを終了します。
2. VR Stick Scopeを起動し、Runtime でエンジン停止を押します。
3. SteamVRを起動し直します。

この公開版にはSteamVR仮想コントローラードライバーは含まれていません。
VRChat OSCへ補正値を送る機能だけを含みます。
